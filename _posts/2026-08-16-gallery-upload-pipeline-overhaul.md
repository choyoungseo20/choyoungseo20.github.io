---
title: 매달 30GB씩 쌓이던 갤러리를 뜯어고치기까지
description: 느리고 비싸던 갤러리 업로드의 병목을 추적해 저장 용량 90%, 업로드 시간 67%를 줄였다. 병렬화로 시작했지만 결정적인 해법은 단계 자체의 제거였다. 최적화의 첫 질문은 "어떻게 빠르게"가 아니라 "이 단계가 왜 있는가"여야 한다는 걸 배운 기록이다.
date: 2026-08-16 09:00:00 +0900
categories: [Backend, Performance]
tags: [s3, upload, webp, deduplication, image-optimization]
---

이력서를 정리하다가 지난 3월에 겪었던 문제를 다시 꺼내 보게 됐다. 당시의 추적 과정을 여기에 정리한다.

학원용 앱에는 선생님이 수업 사진을 올리는 갤러리가 있다. 어느 시점부터 두 가지 신호가 함께 들어왔다. 하나는 체감이었다. 사진 여러 장을 올리면 저장 버튼을 누르고 한참을 기다려야 한다는 것. 다른 하나는 청구서였다. S3 저장량이 매달 30GB씩 늘고 있었고, 스토리지 비용도 같이 우상향 중이었다.

느린 것과 비싼 것. 별개 문제처럼 보였지만, 파이프라인을 추적해보니 뿌리가 같았다.

---

## 기존 구조를 따라가 보다

업로드 흐름은 이랬다.

> 파일 선택 → 원본 그대로 전송 → 1장씩 순차 업로드 → S3 임시 경로(temp/) 보관 → 저장 버튼 → 임시에서 최종 경로로 1장씩 순차 복사 → 게시글 저장

![갤러리 이미지 업로드 최적화 전후 비교](/assets/img/posts/gallery_upload_flow.png)

병목이 겹겹이 쌓여 있었다.

1. 원본 용량 그대로. 요즘 폰 사진은 한 장에 평균 7MB인데, 그게 그대로 전송되고 그대로 저장됐다.
2. 순차 처리. 업로드도 복사도 앞 장이 끝나야 다음 장이 시작됐다. 10장이면 대기 시간도 10배다.
3. 2중 저장. 임시 경로에 한 번, 최종 경로에 한 번. 같은 파일이 S3에 두 번 쓰였다.
4. 중복 저장. 같은 사진을 여러 게시글에 올리면 그때마다 새로 저장됐다. 학원 특성상 같은 단체 사진이 반별 게시판에 반복 게시되는 일이 흔했다.

시간 문제(2)와 용량 문제(1, 3, 4)가 한 파이프라인 안에 얽혀 있었다. 그래서 이 작업은 처음부터 한 파이프라인에 두 개의 목표를 걸고 시작했다.

---

## 1차 개선, 느린 단계를 빠르게

가장 눈에 띄는 병목부터 손댔다. 순차 처리다.

서버의 임시에서 최종으로의 복사를 스토리지의 비동기 API로 전환해 동시 5건씩 병렬 복사로 바꿨다. 전부를 하나의 성공/실패로 묶지 않고 건별로 판정해, 실패한 건만 제외하고 성공분은 반영되도록 했다. 앱 쪽도 동시 3장씩 병렬 업로드하는 워커 큐 방식으로 바꿨다. 동시성 수치는 처음부터 정답이 있었던 게 아니라, 여러 장을 동시에 처리할 때 기기에 걸리는 부하를 보며 낮춰가면서 잡았다.

효과는 분명했다. 10장 저장 시간이 눈에 띄게 줄었다. 그런데 코드를 다듬다 보니 계속 걸리는 질문이 하나 있었다.

"그런데 이 복사, 애초에 왜 하는 거지?"

---

## 빠르게 만든 단계를, 없애다

임시 경로를 거치는 구조를 처음 만들 때는 이유가 있었을 것이다. 글 작성을 취소하면 임시 파일만 버리면 되니까. 하지만 따져보니 그 목적은 임시 경로 없이도 달성할 수 있었다. 업로드 시점에 파일 경로를 확정하고, 게시글 저장 시점에는 그 경로를 DB에 기록하기만 하면 된다. 글을 취소하면 참조되지 않는 파일이 남을 뿐이고, 그건 별도로 정리하면 된다.

그래서 2차 개선은 코드를 추가하는 게 아니라 단계를 삭제하는 것이 됐다. 처음부터 최종 경로로 업로드하도록 바꾸자 임시에서 최종으로의 복사 시간이 통째로 사라졌고(1차에서 열심히 병렬화한 그 단계다), 임시와 최종의 2중 저장도 함께 사라졌다.

1차에서 만든 병렬 복사 최적화는 이렇게 스스로 걷어내게 됐다. 아깝지 않았다면 거짓말이지만, 이게 이 작업에서 얻은 가장 큰 교훈이었다. 느린 단계를 빠르게 만드는 것보다, 그 단계가 왜 존재하는지 묻는 게 먼저다. 병렬화는 "어떻게 빠르게"의 답이고 구조 질문은 "왜 하는가"의 답인데, 후자가 상위 해법이었다.

---

## 용량 축, 줄이고 걸러내기

시간 축과 병행해서 용량 축도 두 가지를 적용했다.

먼저 브라우저에서 WebP로 압축했다. 서버가 아니라 클라이언트에서 압축하기로 했다. canvas에 그려서 WebP로 변환하되, 최대 폭 1080px에서 시작해 결과가 2MB를 넘으면 폭을 한 단계씩 줄여가며 재시도하는 방식이다.

```text
폭 1080px, 품질 0.8로 변환
  → 2MB 이하면 채택
  → 초과면 폭을 한 단계 줄여 재시도 (하한선까지)
```

클라이언트 압축을 택한 이유는 이득이 이중이기 때문이다. 저장 용량만 주는 게 아니라 전송량 자체가 줄어서 업로드 시간도 같이 준다. 7MB를 올려서 서버가 1MB로 줄이는 것과, 처음부터 1MB만 올리는 것의 차이다.

다음으로 지문 기반 중복 제거를 넣었다. 압축된 파일의 SHA-256 해시를 계산해 이미지 지문으로 쓴다. 업로드 전에 이 지문으로 서버에 조회해서, 같은 지문의 파일이 이미 저장돼 있으면 업로드를 아예 생략하고 기존 경로를 재사용한다. 중복 판정 시점을 서버 저장 단계가 아니라 파일 선택 직후로 당긴 게 포인트다. 중복 건은 저장은 물론 네트워크 전송조차 발생하지 않는다.

정리하면 최종 흐름은 이렇다. 파일을 선택하는 순간 브라우저가 압축과 지문 계산을 미리 끝내두고, 저장 버튼을 누르면 새 파일만, 동시 3장씩, 최종 경로로 바로 올라간다. 서버는 경로를 기록할 뿐이다.

---

## 숫자로 확인하다

운영 환경 지표로 확인한 수치다.

| 지표 | 개선 전 | 개선 후 | 감소율 |
|---|---|---|---|
| 사진 1장 평균 저장 용량 | 7MB | 1MB | 약 86% |
| 월 신규 S3 저장량 | 30GB | 3GB | 약 90% |
| 사진 1장 평균 업로드 시간 | 3초 | 1초 | 약 67% |

눈여겨볼 건 첫 줄과 둘째 줄의 배수가 다르다는 점이다. 압축만으로는 7분의 1인데 월 저장량은 10분의 1이 됐다. 그 차이가 중복 제거와 2중 저장 폐지의 몫이다. 개선을 여러 개 겹쳤을 때 총합에서 각각의 기여를 분해해볼 수 있었던 건, 수치를 지표별로 나눠 측정해둔 덕이었다. 업로드 시간은 업로드 함수에 건당 소요시간 로깅을 심어 측정했고, 저장량은 S3 지표의 전후 비교다.

---

## 회고

이 작업에서 가장 크게 남은 건 순서에 대한 아쉬움이다. 병렬화(1차)를 먼저 하고 구조 질문(2차)을 나중에 했는데, 반대였다면 1차 작업의 상당 부분이 필요 없었다. 다음부터 성능 작업은 "이 단계를 없앨 수 있나?"부터 묻고, 없앨 수 없는 단계만 빠르게 만들기로 했다.

클라이언트에 일을 옮기는 게 공짜가 아니라는 것도 확인했다. 압축과 해시 계산이 사용자 기기에서 돌기 때문에 저사양 기기에서의 처리 시간은 지켜봐야 할 부분이다. 다만 파일 선택 직후에 미리 처리해두는 구조라, 사용자가 제목과 내용을 쓰는 동안 백그라운드에서 끝나는 경우가 대부분이었다.

남겨둔 과제도 기록해둔다.

- 구 경로 통합 정리. 구 업로드 경로가 다른 화면에서 아직 쓰이고 있어, 신 구조로의 통합이 남아 있다.
- 미참조 파일 정리. 취소된 글이 남긴 참조되지 않는 파일은 별도 배치로 챙겨야 할 몫이다.
